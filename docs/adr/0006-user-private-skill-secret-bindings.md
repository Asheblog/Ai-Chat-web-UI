---
status: accepted
---

# Skill 密钥采用用户私有绑定

Skill 包和清单只能声明命名密钥需求，不得包含密钥值；用户为自己的已安装 Skill 单独绑定加密保存的私有密钥，且只有同时获批 `secret` 能力和目标网络范围的具体版本能在运行时临时获得对应值。密钥不可回显、记录、导出或跨 Skill 访问，也不得继承模型连接和系统工具密钥；密钥存储使用强制配置的主密钥和带认证加密，禁止使用默认密钥回退。
