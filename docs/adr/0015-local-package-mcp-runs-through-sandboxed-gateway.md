---
status: accepted
---

# 本地包型 MCP 服务通过沙箱网关运行

管理员安装的本地包型 MCP 服务不得由后端在宿主机直接启动，而是统一交给 MCP Gateway 在容器或沙箱边界内托管进程并代理调用；远程 MCP 服务只建立客户端连接，不进入本地部署网关。该决策增加部署与生命周期管理复杂度，换取文件系统、网络、环境变量和密钥注入的集中控制，避免本地 MCP 包成为宿主机任意代码执行入口。
