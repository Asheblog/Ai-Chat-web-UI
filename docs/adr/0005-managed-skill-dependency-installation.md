---
status: accepted
---

# Skill 依赖采用受管构建安装

可执行型 Skill 的 Python、Node 等依赖必须由不可变版本预先声明并锁定，只能在隔离构建阶段通过管理员允许的软件源安装，运行阶段禁止临时执行包管理器。依赖环境按 Skill 版本与目标平台生成并保持只读，不安装到宿主机或共享全局环境；Git URL、任意下载地址和安装脚本默认禁止，确需使用时提升风险并重新审批，以增加构建缓存和多平台管理成本换取可复现性与供应链隔离。
