---
status: accepted
---

# Skill 包按内容复用，安装与运行状态保持隔离

相同 commit SHA 与包哈希对应的不可变 Skill 包采用内容寻址存储并允许多个用户安装记录引用，用户所有权、配置、版本选择和会话绑定保持独立。每次运行使用独立的用户与会话工作区，卸载只移除当前用户的安装关系，共享包在无人引用后回收，以较高的引用管理复杂度换取完整逻辑隔离和更低的磁盘占用。
