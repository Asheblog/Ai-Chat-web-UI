/**
 * Shared model label helpers。
 *
 * 实现已收敛至 @aichat/shared/model-display（backend / frontend 共用）。
 * 本文件保留为兼容再导出。
 */
export {
  collectDuplicateModelNames,
  formatModelOptionLabel,
  formatModelSecondaryLabel,
  modelProviderLabel,
  type ModelDisplaySource,
} from '@aichat/shared/model-display'
