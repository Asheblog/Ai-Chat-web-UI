import type { AxiosInstance } from 'axios'

/**
 * TODO: 后续将在此模块实现聊天相关的 API 封装。
 * 现在仅保留占位，确保 legacy apiClient 在构造时完成引用。
 */
export const registerChatApiPlaceholders = (client: AxiosInstance) => {
  void client
}
