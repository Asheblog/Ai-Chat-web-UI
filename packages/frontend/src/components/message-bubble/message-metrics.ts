/**
 * 消息指标格式化工具。
 *
 * formatDurationMs / formatDurationSeconds 已收敛至 @/lib/format。
 */
export {
  formatDurationMs,
  formatDurationSeconds,
  normalizeMetricMs,
  normalizeMetricNumber,
} from '@/lib/format'
